package com.stretch.repository;

import com.stretch.entity.PoseCheck;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PoseCheckRepository extends JpaRepository<PoseCheck, Long> {
}
